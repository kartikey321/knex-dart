name: knex_dart_live_test
description: >-
  Internal, unpublished framework for knex_dart's live-execution test
  corpus, fixture profiles, and driver adapters. Not a public package.
publish_to: none

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  knex_dart: ^1.3.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.0
