@AGENTS.md

## Claude Code

- Step-by-step skills live in `.claude/skills/` — use them for multi-step tasks instead of expanding this file.
- `/add-driver` — add a new database driver end-to-end
- `/run-tests` — run driver tests locally with Docker
- `/release-package` — release a package or the playground/docs
- `/add-feature` — add a query-building feature across core + docs + playground
