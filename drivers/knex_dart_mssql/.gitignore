# Dart / Flutter tool state
.dart_tool/

# Build outputs
build/

# Library packages should not commit lockfiles.
pubspec.lock

# Flutter tool artifacts (if present)
.flutter-plugins
.flutter-plugins-dependencies

# macOS-only symlinks to FreeTDS libraries — dev-only, must not be committed
libsybdb.dylib
libct.dylib
